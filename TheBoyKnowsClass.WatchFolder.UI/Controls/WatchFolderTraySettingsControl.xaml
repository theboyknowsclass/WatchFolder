<UserControl x:Class="TheBoyKnowsClass.WatchFolder.UI.Controls.WatchFolderTraySettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="53" DataContext="{Binding Path=SystemTrayApplication, RelativeSource={RelativeSource Self}}">
    <Grid>
        <GroupBox Header="Tray Icon Settings" Name="trayIconGroupBox">
            <Grid>
                <CheckBox Content="Minimise on Close" Height="16" Margin="3.5 7 0 3.5" Name="mimimiseOnCloseCheckBox" IsChecked="{Binding MinimiseOnClose}" />
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
